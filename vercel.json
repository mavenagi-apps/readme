{
  "buildCommand": "corepack enable && corepack prepare pnpm@10.3.0 --activate && pnpm run build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.3.0 --activate && pnpm install"
}